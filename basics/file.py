# basic python file
# Line to demonstarate soft reset.
#another line to demonstrate keeping chamges in local file even if you pushed the commit.
# if not pushed yet git reset HEAD~1 --soft
# if pushed revert to last commit , push those changes and then revert again with --no-commit flag with hash of current commit.

# Dev added this
print('i am developer of this application')
